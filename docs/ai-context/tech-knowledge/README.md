# Technical Knowledge

Ingested reference docs and external framework knowledge extracted via `python tools/add_project_doc.py`. Indexed for domain-aware routing; never dumped raw into always-loaded context.
