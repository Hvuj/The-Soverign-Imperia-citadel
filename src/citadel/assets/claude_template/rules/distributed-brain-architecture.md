# Distributed Brain Architecture

Agents decide. Tools execute deterministic updates. Graph stores relationships. Hooks synchronize context.

Start through `scripts/claude-start-smart.sh`. Manual search and run-once sync are debug-only.
