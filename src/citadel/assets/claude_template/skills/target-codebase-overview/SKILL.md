# target-codebase-overview

Use graph-index and brain_query before broad scans. Discover key areas (assets,
features, ops, tests, behavior specs) via `tools/workspace_discoverer.py` /
`.claude/state/workspace-discovery.json` for whichever repo is in scope — never a
hardcoded client path.
